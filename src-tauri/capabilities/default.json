{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: native shell only. No data, auth, or dialog permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
