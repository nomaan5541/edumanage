# EduManage — Master Implementation Status
Per Spec Section 114. Statuses: CURRENT, PARTIAL, REQUIRED, LEGACY, BROKEN, CONFLICTING, DEPRECATED.
A module is only CURRENT after implementation + database verification + authorization verification + error handling + UI verification + acceptance test all pass (Rule 0.15). Do not edit a module's status to CURRENT without noting which checks were actually run.

## Foundation
Authentication — PARTIAL — impl: Supabase Auth via supabase-js, login page, session/role context, sign-out, forgot/reset password flow — db: profiles auto-created via handle_new_user trigger (now includes email) — RLS: profiles/user_roles policies written — roles: all — routes: /login, /forgot-password, /reset-password, /unauthorized, role home redirects — tests: none run (no live DB yet) — issues: not verified against a live Postgres/Auth instance.
Multi-tenancy — PARTIAL — impl: school_id on every tenant table, is_school_member/is_school_admin_or_above helpers — db: schools table + FKs — RLS: written for all foundation tables — roles: all — routes: n/a — tests: none run yet — issues: cross-school RLS (SEC-001/002/003) not yet executed against a real DB.
RBAC — PARTIAL — impl: app_role enum, user_roles table, can_assign_role/has_role_in_school helpers, create_school_bootstrap RPC — db: written — RLS: written — roles: all — routes: n/a — tests: none run yet — issues: same as above.
Sub-Admin Permissions — PARTIAL — impl: permissions catalog + role_permissions table + has_permission() helper + enforce_role_permission_target trigger + invite-school-sub-admin Edge Function + SubAdminsPage UI (invite, per-permission checkbox editor, active/deactivated badge + deactivate/reactivate toggle on user_roles.is_active) — db: written — RLS: written — roles: school_admin, sub_admin — routes: /admin/sub-admins — tests: none run yet (no live DB/Edge Function deploy) — issues: SUB-001..005 untested against a live database.
School Management + Setup Wizard — PARTIAL — impl: Super Admin Schools page (list + create dialog + suspend/activate/mark-expired status controls), create-school-admin Edge Function (invite flow), School Admin Setup Wizard (academic year → classes → sections → subjects), School Admin SchoolProfilePage (edit name/address/contact/board/principal - status remains Super Admin only per the enforce_school_status_change trigger) — db: schools/create_school_bootstrap RPC written — RLS: written — roles: super_admin, school_admin — routes: /super-admin/schools, /admin/setup, /admin/profile — tests: none run yet (no live DB/Edge Function deploy) — issues: Edge Function not deployed/invoked against a real project yet; school status changes are not yet written to audit_logs (only school/school_admin creation is).
Academic Years/Classes/Sections/Subjects — PARTIAL — impl: full CRUD via Setup Wizard + standalone AcademicsPage (tabs for years/classes/sections/subjects + Subject Mapping tab binding subjects to classes per academic year via class_subjects) + School Admin dashboard counts — db: written incl. cross-school consistency triggers — RLS: written — roles: school_admin — routes: /admin, /admin/setup, /admin/academics — tests: none run yet — issues: set-active-year is two sequential client calls, not a single transaction (acceptable for one admin at a time, could race with concurrent admins).
Audit Logs — PARTIAL — impl: write_audit_log() RPC, called from create_school_bootstrap; AuditLogPage viewer UI — db: audit_logs table, append-only grants — RLS: select-only policy written — roles: super_admin, school_admin — routes: /admin/audit-log, /super-admin/audit-log — tests: none run yet — issues: only school/school_admin creation currently writes audit events; other modules' RPCs (owned by Factory tasks) still need to call write_audit_log per docs/conventions/BACKEND_CONVENTIONS.md.

## Student & Teacher
Student Master + Admissions — PARTIAL — impl: `students`/`student_enrollments` tables (enrollment kept separate from identity so promotion never overwrites history), `create_student_admission` RPC (duplicate admission_no check, atomic student+enrollment insert, audit log), Student roster + Admission dialog UI — db: written — RLS: written (self/staff/assignment-scoped teacher read; has_permission-gated writes) — roles: school_admin, sub_admin (with students.* permission) — routes: /admin/students — tests: none run (no live DB in this environment) — issues: built directly by the orchestrator, superseding the in-progress Warp Factory task for this module (see Factory Incident Log) — that Factory task's eventual PR, if any, should be treated as redundant/conflicting and closed rather than merged.
Student Documents/Photo — PARTIAL — impl: `student_documents` table (storage metadata only, no actual file upload UI yet) — db: written — RLS: written — roles: school_admin, sub_admin — routes: none yet (no upload UI) — tests: none run — issues: upload UI and Supabase Storage bucket wiring not yet built.
Student Promotion — PARTIAL — impl: `promote_students` RPC (bulk, marks old enrollment `promoted` + inserts new enrollment row, audit log per student), Promotion tab UI (multi-select + destination year/class/section) — db: written — RLS: n/a (enrollment writes only via RPC) — roles: school_admin, sub_admin (students.update) — routes: /admin/students (Promotion tab) — tests: none run — issues: none known beyond lack of live-DB verification.
Student Transfer — PARTIAL — impl: `student_transfers` table + `initiate_student_transfer`/`accept_student_transfer`/`reject_student_transfer` RPCs (cross-school workflow: source initiates, destination accepts with a new admission_no or rejects) — db: written — RLS: select-only, writes via RPC — roles: school_admin, sub_admin (students.update/students.create) — routes: none yet (no dedicated UI — RPCs exist but aren't wired into a page) — tests: none run — issues: needs a UI (e.g. on the Student roster, an "Initiate transfer" action and a destination-school "Incoming transfers" inbox) — deferred since it's cross-school and lower-frequency than admission/promotion.
Teacher Management + Assignments — PARTIAL — impl: `teachers`/`teacher_assignments` tables, `invite-school-teacher` Edge Function (mirrors invite-school-sub-admin, role=teacher), Teacher list/invite/deactivate + assignment management UI (class/section/subject scoping) — db: written incl. cross-school consistency trigger — RLS: written — roles: school_admin, sub_admin (teachers.* permission) — routes: /admin/teachers — tests: none run — issues: same Factory-supersession note as Student Master above.
Teacher One-Session Security — PARTIAL — impl: `teacher_sessions` table (unique partial index enforcing one active session per teacher), `register_teacher_session` (revoke-and-replace, called on `SIGNED_IN` when the user has a teacher role), `is_teacher_session_active` (polled every 30s + on window focus by `TeacherSessionGuard`, force-signs-out with the spec's exact message "Your teacher account is active on another device."), `revoke_all_teacher_sessions` (not yet wired to a password-reset flow) — db: written — RLS: select-own/admin only, writes via RPC — roles: teacher — routes: /teacher (guard mounted on the whole route) — tests: none run — issues: not wired into a password-reset Edge Function yet; revocation detection is poll-based (up to ~30s delay), not realtime push.

## Financial
Fees/Fee Structures/Payments — PARTIAL — impl: `fee_types`/`fee_structures`/`student_fee_adjustments` tables, `record_fee_payment` RPC (server-computed OUTSTANDING = TOTAL_DUE + FINES - DISCOUNTS - CONCESSIONS - VERIFIED_PAYMENTS, rejects overpayment, `pg_advisory_xact_lock` keyed on student+fee_type+year to serialize concurrent payment races, idempotency via unique `(school_id, idempotency_key)`), `get_student_fee_summary` RPC — db: written incl. cross-school consistency triggers — RLS: written (fee_payments has no direct insert/update policy - RPC only) — roles: school_admin, sub_admin (fees.*), student (self) — routes: /admin/fees (Fee Structure + Record Payment tabs), /student/fees — tests: none run (no live DB in this environment) — issues: this supersedes the now-stopped Warp Factory task for this module (see Factory Incident Log) — no refund/cancellation RPC yet (fee_payments only ever writes status=paid in Phase 1).
Receipts — PARTIAL — impl: server-generated sequential receipt numbers (`RCPT-<school_code>-<seq>` via a locked per-school counter table), receipt display + browser print (`window.print()`) on both the School Admin payment screen and the Student fees page — db: written — issues: not a generated PDF file - relies on the browser's print-to-PDF, no school logo/letterhead layout yet.
Subscription (manual Super Admin activation) — PARTIAL — impl: `subscription_requests` table + `request_subscription_renewal`/`approve_subscription_renewal`/`reject_subscription_renewal` RPCs (School Admin requests, Super Admin manually approves with an expiry date or rejects, per Rule 0.8 addendum - no Stripe/automatic billing), School Admin Subscription tab (status + request form + history), Super Admin SubscriptionRequestsPage (approval queue) — db: written — RLS: select-only, writes via RPC — roles: school_admin (request), super_admin (approve/reject) — routes: /admin/fees (Subscription tab), /super-admin/subscription-requests — tests: none run.

## Assessment/Attendance
Attendance — PARTIAL — impl: `attendance` table (one row per student/day/academic-year, unique constraint prevents duplicates per Spec Section 24), `mark_attendance` RPC (teacher-assignment-scoped or `attendance.create` permission, bulk upsert per class/section/date, re-marking an already-recorded day is logged as a correction via `write_audit_log`), `get_class_attendance` (roster + status for a day), `get_student_attendance_stats` (totals + percentage, optional month filter) — db: written incl. cross-school consistency trigger — RLS: written (no direct insert/update policy - RPC only) — roles: teacher (own assignments), school_admin/sub_admin (attendance.*), student (self) — routes: /teacher/attendance (mark), /admin/attendance (view/correct any class), /student/attendance (own %) — tests: none run (no live DB in this environment) — issues: this supersedes the now-stopped Warp Factory task for this module (see Factory Incident Log); absence notifications (Spec Section 25) not implemented - no notification system exists yet; export not implemented (see Reports & Exports below).
Offline Exams + Marks — PARTIAL — impl: `exams`/`exam_marks` tables (exam_type constrained to FA1/FA2/MID/FA3/FA4/FINAL per Spec Section 21), `create_exam`/`upsert_exam_marks`/`publish_exam_results` RPCs (teacher-assignment-scoped or exams.* permission; publishing requires the dedicated `exams.publish` permission and locks all marks for that exam - Spec Section 23 result locking), `get_student_exam_results` — db: written incl. cross-school consistency trigger — RLS: written (exam schedule visible to the whole school so students see upcoming exams; marks visible only to staff/assignment-scoped teachers/the student once published) — roles: teacher (own assignments), school_admin/sub_admin (exams.*), student (self, published only) — routes: /teacher/exams (create + enter marks + publish), /admin/exams (oversee any class), /student/results — tests: none run (no live DB in this environment) — issues: no question bank/MCQ support (Spec Section 21 lists this as optional-supported, not built); no re-open-after-publish correction flow yet.

## Communication/Admin Tooling
Homework + Study Materials — PARTIAL — impl: `homework`/`study_materials` tables + new `homework.*`/`study_materials.*` permission catalog entries, RLS-gated directly (teacher-assignment-scoped or permission, via the shared `_is_authorized_for_class` helper - no RPC layer needed since there's no multi-step transaction), private `study-materials` Supabase Storage bucket with tenant-scoped path policies (`study-materials/{school_id}/{class_id}/{filename}`) — db: written incl. cross-school consistency trigger — RLS: written (table + storage.objects policies) — roles: teacher (own assignments), school_admin/sub_admin (homework.*/study_materials.* permission, e.g. can post for any class), student (read + download via signed URL) — routes: /teacher/homework (post + upload), /student/homework (view + download) — tests: none run (no live DB/Storage in this environment) — issues: homework attachments are external links only, not uploaded files (only Study Materials use real Storage); no School Admin-specific homework page yet (admins can still use the same RLS via has_permission, just no dedicated UI); no MCQ/submission workflow (Spec Section 28 explicitly says future submission support is optional).
Timetable — PARTIAL — impl: `timetable_slots` (per-school bell schedule: day+period -> time range) + `timetable_entries` tables + new `timetable.*` permission catalog entries — db: written incl. cross-school consistency trigger; conflict detection (Spec Section 27: no teacher/room/class+section double-booking) is enforced by three unique constraints/index at the database level, not application logic — `create_timetable_entry` RPC wraps the insert to translate the resulting `unique_violation` into a human-readable message per constraint — RLS: select is_school_member (everyone sees the timetable); writes gated by `timetable.*` permission — roles: school_admin/sub_admin (build), teacher/student (read-only, own class/assignments) — routes: /admin/timetable (builder), /teacher/timetable, /student/timetable — tests: none run (no live DB in this environment, so the unique-constraint conflict detection has not actually been exercised) — issues: every timetable_entries row requires a non-null section_id (each section has its own timetable row, no "whole class" entries) - documented modeling choice, not a spec deviation.
Notifications (in-app/email; SMS/push stubbed) — REQUIRED — same Factory task as above.
Backup/Restore — REQUIRED — same Factory task as above.
Reports & Exports — REQUIRED — same Factory task as above.
Super Admin Platform Dashboard — REQUIRED — same Factory task as above.
PWA/offline support — PARTIAL — impl: Vite PWA plugin configured (manifest, safe-caching denylist for API/auth/storage/functions routes), branded SVG app icon (any/maskable), InstallPromptButton wired into AppShell — db: n/a — RLS: n/a — roles: all — routes: n/a — tests: manual install flow not yet verified in a real browser — issues: icon is a placeholder monogram (SVG), not designed PNG/maskable assets - swap in real brand icons later; offline indication UI not built.

## Phase 2 (in progress via Warp Factory, dispatched 2026-09-15)
Meetings + School Calendar — REQUIRED — dispatched (run https://platform.warp.dev/sLAxqZv4PNlhFvGfh0jCrk/runs/01a0a627-e619-7540-80c2-bb57eaff6b98). Independent of unmerged Phase 1 tables.
ID Card Studio (template engine foundation, 5-10 real starter templates, no student data-binding yet) — REQUIRED — dispatched (run https://platform.warp.dev/sLAxqZv4PNlhFvGfh0jCrk/runs/01a0a628-2079-7173-81d4-f7d9928cd5ef). Data-binding to real students deferred until Students & Teachers task merges.
Tauri desktop shell — PARTIAL (merged to main) — impl: official Tauri 2 window around the existing Vite/React app (`src-tauri/` loads `../dist`; `npm run tauri:dev` / `tauri:build`); 1280×800 window with min 1024×640; placeholder icons; Rust is window-only (no Supabase, RLS, or permission checks) — db: n/a — RLS: n/a (same server-side rules as web) — roles: all (same routes as web, including `/login`) — tests: `npm run test:desktop` (config validation); `npm run build` and `npm run lint` for the web target — issues: not CURRENT. No hosted Supabase project / `.env`, so live login-to-API was not proven. Bundles are unsigned (no Apple/Windows signing). A Linux `tauri build` in the building task produced unsigned deb/rpm/AppImage; the window titled EduManage opened, but the webview body was blank so the login form was not proven. CSP was since set to Tauri's default `null` (strict `script-src 'self'` is the likely blank-screen cause) and Tauri Vite builds use relative `base`. That sandbox had no `rustc`/`cargo`; the binary was not rebuilt there. Do not treat scaffolding as a signed installer or as a proven login UI.

## Phase 2 (blocked on Phase 1 merge, not yet dispatched)
Online Exam Engine — REQUIRED — depends on Student + offline Exam tables from the still-in-progress Students & Teachers / Attendance & Offline Exams Factory tasks. Dispatch once those merge to main.
Report Cards (non-AI) — REQUIRED — depends on Student + Marks/Attendance data. Dispatch once Phase 1 merges.

## Deferred (Phase 2+, not started, intentionally out of scope)
Online Exam Engine, Report Cards & AI Report Cards, Face Attendance, Meetings + School Calendar, ID Card Studio, Student AI Assistant, AI School Analytics, Google Workspace integration, Flutter Android app, automatic/Stripe subscription billing.

## Explicitly declined for now (product owner decision, 2026-09-15)
Student AI Assistant, AI Report Cards, AI School Analytics — SKIPPED — requires a real LLM provider API key the product owner has not provided; revisit once one is available and the underlying student/exam data exists.
Google Workspace integration (Calendar/Drive/Docs/Sheets) — SKIPPED — requires a Google Cloud OAuth app/credentials not yet created.
Automatic/Stripe subscription billing — SKIPPED BY DESIGN — Phase 1 intentionally uses manual Super-Admin-only activation instead (Rule 0.8 addendum); do not add Stripe checkout without an explicit future request + real Stripe keys.
Face Attendance — SKIPPED — biometric data has real privacy/legal implications the product owner chose not to take on now; manual/QR attendance (in the Attendance & Offline Exams task) covers the need.
Flutter Android app — SKIPPED FOR NOW — separate native codebase/toolchain; product owner wants the web app finished and stabilized first.

## Factory Incident Log
2026-09-15/17: All 7 dispatched Factory tasks stalled for ~43 hours with zero progress. Root cause diagnosis (via get_task/get_conversation on each task):
- Students & Teachers, ID Card Studio, Tauri packaging: Implement runs FAILED shortly after reaching a live-database verification step (`npx supabase db reset`) or (for Tauri) likely a Rust/Cargo toolchain step — consistent with the sandbox lacking Docker/the toolchain, the same constraint hit locally during Phase 1 kickoff.
- Fees & Subscriptions: Foreman correctly identified that Phase 1 fees are student-scoped but `public.students` doesn't exist yet (Students & Teachers hadn't merged) and asked whether to build a minimal student stub or wait — BLOCKED awaiting an answer.
- Attendance & Offline Exams, Communication & Admin Tooling, Meetings + Calendar: Foreman triaged correctly, recommended a technical spec given cross-module dependencies/ambiguities, and asked for approval to proceed — each ended its run waiting for a reply (shows as "succeeded" but idle, not actually stalled/broken).
Recovery: answered all 4 pending questions via `message_foreman` (told Fees to wait on Students & Teachers rather than build a duplicate table; approved specs for Attendance and Communication with concrete decisions on Communication's 8 open ambiguities; told Meetings to skip the spec and proceed on defaults) and resumed the 3 failed tasks via `send_task` handback with corrected guidance: don't stall/fail when Docker or a toolchain is unavailable in the sandbox — do best-effort static verification, document honestly what couldn't be tested (PARTIAL not CURRENT per Rule 0.15), and still open the PR.
Confirmed 2026-09-17: Tauri packaging PR (#6) landed on main after conflict resolution. The other 6 tasks are actively INPROGRESS (verified via get_task on each) — Fees/Attendance/Communication/Meetings advanced past their questions into spec/implement, and Students & Teachers/ID Card Studio show live Foreman + Implement runs again after the handback. Lesson learned: a Factory task showing "succeeded"/idle or an old `updated_at` does not mean it's making progress — it may be silently waiting on a question. Check `get_task`/`get_conversation` for the actual run states and last agent_output, not just the task list, when following up.
2026-09-17: Product owner asked to stop relying on the Factory (credit cost) and have the orchestrator build the remaining Phase 1 modules directly instead. Already-running Factory tasks are left to finish on their own (sunk cost), but no new Factory tasks will be dispatched, and remaining Phase 1 work is being implemented directly starting now.
2026-09-17: Students & Teachers module built directly on `main` (see the Student & Teacher section above), superseding the still-running Factory task for it (run 01a0a622-91d2-7755-ba6d-4ff8bdfc10d1). If that Factory task still opens a PR later, close it as redundant rather than merging it — the schema/RPC/UI it would add now conflicts with what's already on `main`. Fees & Subscriptions and Attendance & Offline Exams (both still Factory-in-progress) depend on the `students`/`teachers` tables that now exist on `main`; check their PRs for schema conflicts before merging.

## Environment Notes
- Frontend foundation (Vite/React/TS/Tailwind/shadcn-style UI/routing/auth context) builds cleanly: `npm run build` and `npm run lint` both pass as of this writing.
- No Docker / local Postgres detected in the dev environment — Supabase CLI (`npx supabase`) works, but `supabase start`/`db reset` require Docker Desktop, which is not installed. Migrations are being written and reviewed but NOT executed/verified against a real Postgres instance yet. This must be resolved (install Docker Desktop, or connect a hosted Supabase project) before any module can be marked CURRENT, per Rule 0.15.
- npm/npx must be invoked as `npm.cmd`/`npx.cmd` in PowerShell on this machine — the `.ps1` shims are blocked by the execution policy.
- shadcn/ui CLI (`npx shadcn init`) fails with a workspace-config-loading error on this environment across multiple versions (latest, 4.20.0, and Tailwind v4 incompatibility on 2.3.0) — UI primitives in `src/components/ui` are hand-authored shadcn-equivalent code instead.
